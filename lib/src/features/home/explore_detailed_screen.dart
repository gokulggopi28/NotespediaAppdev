import 'package:notespedia/utils/constants/app_export.dart';

class ExploreDetailedScreen extends StatelessWidget {
  const ExploreDetailedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ExploreDetailedScreen"),
      ),
    );
  }
}
